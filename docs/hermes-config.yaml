mcp_servers:
  agent_proteus:
    command: "python"
    args:
      - "-m"
      - "agent_proteus"
      - "mcp"
    timeout: 180
    connect_timeout: 60

# Optional when tools are not on PATH:
# mcp_servers:
#   agent_proteus:
#     command: "python"
#     args: ["-m", "agent_proteus", "mcp"]
#     env:
#       PROTEUS_BRIDGE: "C:/path/to/proteus-bridge.cmd"
#       DESKTOPCTL: "C:/path/to/desktopctl.exe"
#       UI_AUTOMATA_URL: "http://127.0.0.1:7860"
